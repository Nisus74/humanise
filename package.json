{
  "name": "humanise",
  "version": "1.0.0",
  "description": "Open-source AI writing skill that preserves meaning and learns your voice across Claude Code, Codex, Cursor, Gemini, Copilot, OpenCode and Antigravity.",
  "author": "Nisus74",
  "license": "MIT",
  "homepage": "https://github.com/Nisus74/humanise",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Nisus74/humanise.git"
  },
  "keywords": [
    "writing",
    "ai",
    "anti-slop",
    "agent-skills",
    "ai-writing",
    "skills",
    "voice",
    "editor",
    "humanise",
    "writing-assistant",
    "claude",
    "codex",
    "gemini",
    "antigravity"
  ],
  "type": "module",
  "engines": {
    "node": ">=18"
  },
  "bin": {
    "humanise": "cli/bin/cli.js"
  },
  "files": [
    "cli/",
    "dist/",
    ".claude-plugin/",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "node scripts/build.mjs",
    "check:version": "node scripts/check-version-sync.mjs",
    "validate": "node scripts/validate-skill.mjs",
    "check:package-privacy": "node scripts/check-package-privacy.mjs",
    "prepack": "npm run check:version && npm run check:deps && npm run build && npm run validate && npm run check:package-privacy",
    "test:node": "node --test tests/*.test.mjs",
    "test:engine": "cd skill/evals/assertions && python3 selftest.py",
    "test": "npm run build && npm run test:node && npm run test:engine",
    "check:deps": "node scripts/check-no-deps.mjs",
    "quality": "npm run check:version && npm run check:deps && npm run build && npm run validate && npm run check:package-privacy && npm test"
  }
}
