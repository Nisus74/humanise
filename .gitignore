# macOS
.DS_Store
.AppleDouble
.LSOverride

# Editor
.vscode/
.idea/
*.swp
*.swo
*~

# Node (if tooling is added later)
node_modules/
npm-debug.log*

# Env
.env
.env.local

# Python
__pycache__/
*.py[cod]
.ruff_cache/
.pytest_cache/

# A user's filled-in profile and config are local, not committed upstream.
# (Fork and keep your own; the repo ships profile.template and profile.example.)
skill/profile/
skill/config.yml

# Build output (generated by `npm run build`; built on demand and at publish)
/dist/

# Personal, per-developer Claude guidance (not shared)
CLAUDE.local.md
